Compress the following conversation history into a concise summary.
Keep: user intent, completed operations, key decisions, current progress.
Ignore: specific code details, tool output details, intermediate debugging steps.
Reply in the same language as the conversation.

{{.Content}}
